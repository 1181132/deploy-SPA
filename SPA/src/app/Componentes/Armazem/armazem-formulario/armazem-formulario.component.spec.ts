/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ArmazemFormularioComponent } from './armazem-formulario.component';

describe('ArmazemFormularioComponent', () => {
  let component: ArmazemFormularioComponent;
  let fixture: ComponentFixture<ArmazemFormularioComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ArmazemFormularioComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ArmazemFormularioComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
